name: Auto-merge Exercism app PRs

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  automerge:
    runs-on: ubuntu-latest
    steps:
      - name: DEBUG — dump context
        run: |
          echo "event_name: ${{ github.event_name }}"
          echo "actor: ${{ github.actor }}"
          echo "pr login: ${{ github.event.pull_request.user.login }}"
          echo "pr type: ${{ github.event.pull_request.user.type }}"
